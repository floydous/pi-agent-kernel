export const escaped = /\{([^}]+)\}/;
export const classSlash = /[a/z]+/;
export const ratio = 1 / 2;
export const afterComment = 10 /* comment */ / 2;
export const templated = `value ${1 + 2}`;
export const marker = "before";
